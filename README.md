# envsync

One command to set up and run your local development environment.

```bash
envsync init     # create envsync.yaml from your project
envsync up       # create venv + install dependencies
envsync run dev  # run your app with the right environment
envsync doctor   # check what's missing
```
